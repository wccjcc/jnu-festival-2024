spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/
    username: postgres
    password: 641018

  jpa:
    hibernate:
    ddl-auto: create
    show-sql: true
    database: postgresql